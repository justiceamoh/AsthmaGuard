<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@drawable/goodhealth"
    tools:context=".MainActivity">

    <Button
        android:layout_width="30dp"
        android:layout_height="60dp"
        android:id="@+id/btn_recorder"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:layout_width="30dp"
        android:layout_height="60dp"
        android:id="@+id/btn_charts"
        android:layout_above="@+id/btn_recorder"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="20dp"
        android:layout_alignLeft="@+id/btn_recorder"
        android:layout_alignStart="@+id/btn_recorder" />

    <Button
        android:layout_width="30dp"
        android:layout_height="60dp"
        android:id="@+id/btn_add"
        android:layout_marginBottom="20dp"
        android:layout_above="@+id/btn_charts"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />
</RelativeLayout>
